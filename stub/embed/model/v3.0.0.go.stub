package model

import (
	"{{$.GoyaveModVersion}}/database"
	"gorm.io/gorm"
)

func init() {
	database.RegisterModel(&{{$.ModelName}}{})
}

type {{$.ModelName}} struct {
	ID uint `gorm:"primarykey"`
}
